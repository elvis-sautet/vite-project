name: Test work flow

on:
  push:
    branches:
      - master
  workflow_dispatch:
  
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - with:
          path: "test file"
      - run:
          echo "hello worlds"

   
